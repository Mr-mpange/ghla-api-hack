# 🔄 System Workflow Documentation

Complete workflow diagrams for the WhatsApp Micro-Sales Assistant.

---

## 📱 Customer Journey Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    CUSTOMER WHATSAPP JOURNEY                     │
└─────────────────────────────────────────────────────────────────┘

1. GREETING
   Customer: "Hi"
   ↓
   System: Welcome message with buttons
   [🛍️ View Products] [🎉 View Promotions]

2. PRODUCT SELECTION
   Customer: Clicks "View Products"
   ↓
   System: Interactive product list
   ☕ Premium Coffee - KES 1500
   🍯 Organic Honey - KES 800
   🧼 Handmade Soap - KES 600
   ...

3. QUANTITY INPUT
   Customer: Selects "Premium Coffee"
   ↓
   System: "Please enter quantity (e.g., 2)"
   ↓
   Customer: "2"

4. ADDRESS COLLECTION
   System: "Please provide delivery address or reply 'skip'"
   ↓
   Customer: "123 Main St, Nairobi"

5. PAYMENT METHOD
   System: Order summary + payment buttons
   💰 Total: KES 3000
   [📱 M-Pesa] [💳 Card]

6. PAYMENT PROCESSING
   Customer: Clicks "M-Pesa"
   ↓
   System: "Processing payment..."
   ↓
   Ghala: Sends M-Pesa prompt to customer
   ↓
   Customer: Enters PIN on phone

7. RECEIPT
   System: Payment successful! 🎉
   ━━━━━━━━━━━━━━━━━━━━
   RECEIPT
   ━━━━━━━━━━━━━━━━━━━━
   Order ID: ORD-123
   Product: Premium Coffee x2
   Total: KES 3000
   Transaction: MPESA123456
   ━━━━━━━━━━━━━━━━━━━━
   Thank you! 🙏
```

---

## 🔄 Complete System Flow

```
┌──
────────────┐
│   CUSTOMER   │
│  (WhatsApp)  │
└──────┬───────┘
       │ 1. Sends message "Hi"
       ↓
┌──────────────────────────────────────────────────────────────┐
│                    GHALA API PLATFORM                         │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Receives WhatsApp message                             │  │
│  │  Forwards to webhook                                   │  │
│  └────────────────────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────────────────────┘
       │ 2. POST /api/messages/incoming
       ↓
┌──────────────────────────────────────────────────────────────┐
│              OUR NODE.JS BACKEND SERVER                       │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  messageController.handleIncomingMessage()          │    │
│  │  - Parse message                                    │    │
│  │  - Check user state                                 │    │
│  │  - Route to appropriate handler                     │    │
│  └─────────────────────────────────────────────────────┘    │
│                          ↓                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  whatsappService.sendGreeting()                     │    │
│  │  - Generate greeting message                        │    │
│  │  - Create interactive buttons                       │    │
│  └─────────────────────────────────────────────────────┘    │
└──────┬────────────────────────────────────────────────────────┘
       │ 3. Call Ghala API to send message
       ↓
┌──────────────────────────────────────────────────────────────┐
│                    GHALA API PLATFORM                         │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  POST /whatsapp/messages                               │  │
│  │  - Validate request                                    │  │
│  │  - Send to WhatsApp Business API                      │  │
│  └────────────────────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────────────────────┘
       │ 4. Deliver message
       ↓
┌──────────────┐
│   CUSTOMER   │
│  Receives:   │
│  [View       │
│   Products]  │
│  [View       │
│   Promotions]│
└──────────────┘
```

---

## 💳 Payment Processing Flow

```
STEP
 1: ORDER CREATION
┌──────────────┐
│   Customer   │ Selects product, enters quantity & address
└──────┬───────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  orderController.createOrder()                          │
│  1. Validate input                                      │
│  2. Find/create customer in database                    │
│  3. Calculate total amount                              │
│  4. Create order record                                 │
│  5. Send payment options to customer                    │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  DATABASE                                               │
│  ┌───────────────────────────────────────────────────┐ │
│  │ INSERT INTO orders (                              │ │
│  │   id, customer_id, product_id, quantity,          │ │
│  │   total_amount, status='pending'                  │ │
│  │ )                                                 │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘

STEP 2: PAYMENT INITIATION
┌──────────────┐
│   Customer   │ Clicks "M-Pesa" button
└──────┬───────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  paymentService.initiatePayment()                       │
│  1. Get order details from database                     │
│  2. Create payment order in Ghala                       │
│  3. Save payment record in database                     │
│  4. Send confirmation to customer                       │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  GHALA API                                              │
│  POST /orders                                           │
│  {                                                      │
│    "amount": 3000,                                      │
│    "currency": "KES",                                   │
│    "payment_method": "mpesa",                           │
│    "customer": { "phone": "+254700123456" }             │
│  }                                                      │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  M-PESA SYSTEM                                          │
│  - Sends STK Push to customer phone                     │
│  - Customer enters PIN                                  │
│  - Processes payment                                    │
└─────────────────────────────────────────────────────────┘

STEP 3: PAYMENT CONFIRMATION (Webhook)
┌─────────────────────────────────────────────────────────┐
│  GHALA API                                              │
│  Payment successful!                                    │
│  Sends webhook event                                    │
└─────────────────────────────────────────────────────────┘
       │
       ↓ POST /api/webhooks/ghala
┌─────────────────────────────────────────────────────────┐
│  webhookController.handlePaymentSuccess()               │
│  1. Verify webhook signature (security)                 │
│  2. Parse event data                                    │
│  3. Update payment status in database                   │
│  4. Update order status to 'completed'                  │
│  5. Send receipt to customer                            │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  DATABASE                                               │
│  UPDATE orders SET                                      │
│    status='completed',                                  │
│    payment_status='paid'                                │
│  WHERE id='ORD-123'                                     │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌──────────────┐
│   Customer   │ Receives receipt via WhatsApp
└──────────────┘
```

---

## 🔐 Webhook Security Flow

```
┌─
────────────────────────────────────────────────────────┐
│  GHALA API                                              │
│  1. Creates webhook payload                             │
│  2. Generates HMAC SHA256 signature                     │
│     signature = HMAC-SHA256(payload, webhook_secret)    │
│  3. Sends POST request with signature header            │
└─────────────────────────────────────────────────────────┘
       │
       ↓ POST /api/webhooks/ghala
       │ Headers: x-ghala-signature: abc123...
       │ Body: {"event":"payment.success","data":{...}}
       │
┌─────────────────────────────────────────────────────────┐
│  OUR SERVER - webhookRoutes.js                          │
│                                                         │
│  1. Receive raw request body                            │
│  2. Extract signature from header                       │
│  3. Call webhookVerifier.verifyWebhookSignature()       │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  webhookVerifier.js                                     │
│                                                         │
│  const expectedSignature = crypto                       │
│    .createHmac('sha256', WEBHOOK_SECRET)                │
│    .update(payload)                                     │
│    .digest('hex');                                      │
│                                                         │
│  if (signature === expectedSignature) {                 │
│    return true; ✅                                      │
│  } else {                                               │
│    return false; ❌                                     │
│  }                                                      │
└─────────────────────────────────────────────────────────┘
       │
       ↓
┌─────────────────────────────────────────────────────────┐
│  IF VALID ✅                                            │
│  - Process webhook event                                │
│  - Update database                                      │
│  - Send notifications                                   │
│  - Return 200 OK                                        │
│                                                         │
│  IF INVALID ❌                                          │
│  - Log security warning                                 │
│  - Reject request                                       │
│  - Return 401 Unauthorized                              │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Database Operations Flow

```
N
EW CUSTOMER ORDER
┌──────────────────────────────────────────────────────────┐
│  1. Customer sends first message                         │
└──────────────────────────────────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────────────────┐
│  customerModel.findOrCreateCustomer(phoneNumber)         │
│                                                          │
│  SELECT * FROM customers                                 │
│  WHERE phone_number = '+254700123456'                    │
│                                                          │
│  IF NOT FOUND:                                           │
│    INSERT INTO customers (phone_number, name)            │
│    VALUES ('+254700123456', 'John Doe')                  │
│                                                          │
│  RETURN customer object                                  │
└──────────────────────────────────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────────────────┐
│  orderModel.createOrder(orderData)                       │
│                                                          │
│  INSERT INTO orders (                                    │
│    id, customer_id, product_id, product_name,            │
│    quantity, unit_price, total_amount,                   │
│    currency, delivery_address, status                    │
│  ) VALUES (                                              │
│    'ORD-123', 1, 'prod_001', 'Coffee',                   │
│    2, 1500, 3000, 'KES', '123 Main St', 'pending'        │
│  )                                                       │
└──────────────────────────────────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────────────────┐
│  paymentService.createPaymentRecord()                    │
│                                                          │
│  INSERT INTO payments (                                  │
│    order_id, amount, currency,                           │
│    payment_method, status                                │
│  ) VALUES (                                              │
│    'ORD-123', 3000, 'KES', 'mpesa', 'pending'            │
│  )                                                       │
└──────────────────────────────────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────────────────┐
│  PAYMENT SUCCESSFUL (via webhook)                        │
│                                                          │
│  UPDATE payments                                         │
│  SET status='success', transaction_ref='MPESA123'        │
│  WHERE order_id='ORD-123'                                │
│                                                          │
│  UPDATE orders                                           │
│  SET status='completed', payment_status='paid'           │
│  WHERE id='ORD-123'                                      │
└──────────────────────────────────────────────────────────┘

DATABASE RELATIONSHIPS
┌─────────────────┐
│   customers     │
│  ─────────────  │
│  id (PK)        │
│  phone_number   │
│  name           │
└────────┬────────┘
         │ 1
         │
         │ N
┌────────┴────────┐
│     orders      │
│  ─────────────  │
│  id (PK)        │
│  customer_id(FK)│
│  product_id     │
│  total_amount   │
│  status         │
└────────┬────────┘
         │ 1
         │
         │ N
┌────────┴────────┐
│    payments     │
│  ─────────────  │
│  id (PK)        │
│  order_id (FK)  │
│  amount         │
│  status         │
└─────────────────┘
```

---

## 🎯 Message Handling State Machine

```
USER ST
ATE TRANSITIONS

                    ┌─────────────┐
                    │  GREETING   │ ← Initial state
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │                         │
    "View Products"            "View Promotions"
              │                         │
              ↓                         ↓
    ┌─────────────────┐        ┌──────────────┐
    │ PRODUCT_SELECTED│        │   GREETING   │
    └────────┬────────┘        └──────────────┘
             │
    User selects product
             │
             ↓
    ┌─────────────────┐
    │ QUANTITY_INPUT  │
    └────────┬────────┘
             │
    User enters "2"
             │
             ↓
    ┌─────────────────┐
    │ ADDRESS_INPUT   │
    └────────┬────────┘
             │
    User enters address
             │
             ↓
    ┌─────────────────┐
    │ PAYMENT_        │
    │ SELECTION       │
    └────────┬────────┘
             │
    User selects M-Pesa
             │
             ↓
    ┌─────────────────┐
    │ PAYMENT_        │
    │ PROCESSING      │
    └────────┬────────┘
             │
    Payment webhook received
             │
             ↓
    ┌─────────────────┐
    │   COMPLETED     │
    └─────────────────┘
             │
    State cleared
             │
             ↓
    ┌─────────────────┐
    │   GREETING      │ ← Ready for next order
    └─────────────────┘

STATE STORAGE (In-Memory Map)
┌──────────────────────────────────────────────────────┐
│  userStates = new Map()                              │
│                                                      │
│  Key: "+254700123456"                                │
│  Value: {                                            │
│    step: "quantity_input",                           │
│    selectedProduct: "prod_001",                      │
│    quantity: null,                                   │
│    orderId: null                                     │
│  }                                                   │
└──────────────────────────────────────────────────────┘

NOTE: In production, use Redis for state management
```

---

## 🔄 Admin Dashboard Data Flow

```

┌──────────────────────────────────────────────────────────┐
│  BROWSER - Admin Dashboard (index.html)                  │
│  http://localhost:3000/admin                             │
└──────────────────────────────────────────────────────────┘
       │
       │ 1. Page loads
       ↓
┌──────────────────────────────────────────────────────────┐
│  JavaScript: loadDashboard()                             │
│  - Calls loadStats()                                     │
│  - Calls loadOrders()                                    │
│  - Calls loadCustomers()                                 │
└──────────────────────────────────────────────────────────┘
       │
       ↓ GET /api/admin/stats (with Basic Auth)
┌──────────────────────────────────────────────────────────┐
│  adminRoutes.js                                          │
│  1. Verify authentication                                │
│  2. Query database for statistics                        │
└──────────────────────────────────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────────────────┐
│  DATABASE QUERIES                                        │
│                                                          │
│  SELECT COUNT(*) FROM orders                             │
│  SELECT COUNT(*) FROM orders WHERE status='completed'    │
│  SELECT SUM(total_amount) FROM orders WHERE paid         │
│  SELECT COUNT(*) FROM customers                          │
│  SELECT COUNT(*) FROM orders WHERE status='pending'      │
└──────────────────────────────────────────────────────────┘
       │
       ↓ Return JSON
┌──────────────────────────────────────────────────────────┐
│  {                                                       │
│    "totalOrders": 150,                                   │
│    "completedOrders": 120,                               │
│    "totalRevenue": 450000,                               │
│    "totalCustomers": 75,                                 │
│    "pendingOrders": 30                                   │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────────────────┐
│  BROWSER - Updates UI                                    │
│  - Total Revenue: KES 450,000                            │
│  - Total Orders: 150                                     │
│  - Total Customers: 75                                   │
│  - Pending Orders: 30                                    │
│                                                          │
│  Auto-refreshes every 30 seconds                         │
└──────────────────────────────────────────────────────────┘
```

---

## 🏗️ System Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   WhatsApp   │  │  Ghala API   │  │   M-Pesa     │      │
│  │   Business   │  │   Platform   │  │   Gateway    │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
         │                    │                    │
         │                    │                    │
         └────────────────────┼────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │   INTERNET        │
                    └─────────┬─────────┘
                              │
         ┌────────────────────┼────────────────────┐
         │                    │                    │
         ↓                    ↓                    ↓
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  Webhook        │  │  Webhook        │  │  API Calls      │
│  /messages/     │  │  /webhooks/     │  │  (Outgoing)     │
│  incoming       │  │  ghala          │  │                 │
└────────┬────────┘  └────────┬────────┘  └────────┬────────┘
         │                    │                    │
         └────────────────────┼────────────────────┘
                              │
         ┌────────────────────┴────────────────────┐
         │                                         │
         │        OUR NODE.JS APPLICATION          │
         │                                         │
         │  ┌─────────────────────────────────┐   │
         │  │         ROUTES LAYER            │   │
         │  │  - webhookRoutes                │   │
         │  │  - messageRoutes                │   │
         │  │  - orderRoutes                  │   │
         │  │  - adminRoutes                  │   │
         │  └────────────┬────────────────────┘   │
         │               │                         │
         │  ┌────────────┴────────────────────┐   │
         │  │      CONTROLLERS LAYER          │   │
         │  │  - webhookController            │   │
         │  │  - messageController            │   │
         │  │  - orderController              │   │
         │  └────────────┬────────────────────┘   │
         │               │                         │
         │  ┌────────────┴────────────────────┐   │
         │  │       SERVICES LAYER            │   │
         │  │  - ghalaService                 │   │
         │  │  - whatsappService              │   │
         │  │  - paymentService               │   │
         │  └────────────┬────────────────────┘   │
         │               │                         │
         │  ┌────────────┴────────────────────┐   │
         │  │        MODELS LAYER             │   │
         │  │  - orderModel                   │   │
         │  │  - customerModel                │   │
         │  └────────────┬────────────────────┘   │
         │               │                         │
         └───────────────┼─────────────────────────┘
                         │
                         ↓
         ┌───────────────────────────────┐
         │      SQLite DATABASE          │
         │  ┌─────────────────────────┐  │
         │  │  customers              │  │
         │  │  orders                 │  │
         │  │  payments               │  │
         │  └─────────────────────────┘  │
         └───────────────────────────────┘
```

---

## 📦 Request/Response Flow Examples

### Example 1: Create Order via API

```
REQUEST
┌──────────────────────────────────────────────────────────┐
│  POST /api/orders                                        │
│  Content-Type: application/json                          │
│                                                          │
│  {                                                       │
│    "phoneNumber": "+254700123456",                       │
│    "productId": "prod_001",                              │
│    "quantity": 2,                                        │
│    "deliveryAddress": "123 Main St",                     │
│    "customerName": "John Doe"                            │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
       │
       ↓ orderController.createOrder()
┌──────────────────────────────────────────────────────────┐
│  PROCESSING                                              │
│  1. Validate: phoneNumber, productId, quantity ✓         │
│  2. Find product in catalog ✓                            │
│  3. Find/create customer in DB ✓                         │
│  4. Calculate total: 1500 × 2 = 3000 ✓                   │
│  5. Create order in DB ✓                                 │
│  6. Send payment options via WhatsApp ✓                  │
└──────────────────────────────────────────────────────────┘
       │
       ↓
RESPONSE
┌──────────────────────────────────────────────────────────┐
│  HTTP 201 Created                                        │
│                                                          │
│  {                                                       │
│    "success": true,                                      │
│    "message": "Order created successfully",              │
│    "data": {                                             │
│      "id": "ORD-1705401234567-abc123",                   │
│      "customerId": 1,                                    │
│      "productName": "Premium Coffee Beans 1kg",          │
│      "quantity": 2,                                      │
│      "totalAmount": 3000,                                │
│      "currency": "KES"                                   │
│    }                                                     │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
```

### Example 2: Webhook Event Processing

```
INCOMING WEBHOOK
┌──────────────────────────────────────────────────────────┐
│  POST /api/webhooks/ghala                                │
│  x-ghala-signature: abc123def456...                      │
│  Content-Type: application/json                          │
│                                                          │
│  {                                                       │
│    "event": "payment.success",                           │
│    "data": {                                             │
│      "order_id": "ghala_order_123",                      │
│      "transaction_reference": "MPESA123456",             │
│      "amount": 3000,                                     │
│      "currency": "KES",                                  │
│      "status": "completed"                               │
│    }                                                     │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
       │
       ↓ 1. Verify signature
┌──────────────────────────────────────────────────────────┐
│  webhookVerifier.verifyWebhookSignature()                │
│  ✅ Signature valid                                      │
└──────────────────────────────────────────────────────────┘
       │
       ↓ 2. Route event
┌──────────────────────────────────────────────────────────┐
│  webhookController.handlePaymentSuccess()                │
│  - Get payment from DB                                   │
│  - Get order from DB                                     │
│  - Update payment status: 'success'                      │
│  - Update order status: 'completed'                      │
│  - Send receipt to customer                              │
└──────────────────────────────────────────────────────────┘
       │
       ↓ 3. Send receipt
┌──────────────────────────────────────────────────────────┐
│  whatsappService.sendPaymentReceipt()                    │
│  → ghalaService.sendWhatsAppMessage()                    │
│  → Ghala API → WhatsApp → Customer                       │
└──────────────────────────────────────────────────────────┘
       │
       ↓
RESPONSE
┌──────────────────────────────────────────────────────────┐
│  HTTP 200 OK                                             │
│                                                          │
│  {                                                       │
│    "success": true,                                      │
│    "message": "Webhook processed successfully"           │
│  }                                                       │
└──────────────────────────────────────────────────────────┘
```

---

## 🔄 Error Handling Flow

```
ERROR SCENARIOS

1. INVALID PRODUCT
   Customer selects invalid product
   ↓
   System checks product catalog
   ↓
   Product not found ❌
   ↓
   Send message: "Sorry, product not found"
   ↓
   Return to greeting state

2. PAYMENT FAILURE
   Customer initiates payment
   ↓
   Ghala processes payment
   ↓
   Payment fails (insufficient funds)
   ↓
   Webhook: payment.failed
   ↓
   Update order: status='payment_failed'
   ↓
   Send message: "Payment failed. Please try again"
   ↓
   Offer retry option

3. WEBHOOK SIGNATURE INVALID
   Ghala sends webhook
   ↓
   Verify signature
   ↓
   Signature mismatch ❌
   ↓
   Log security warning
   ↓
   Return 401 Unauthorized
   ↓
   Do not process event

4. DATABASE ERROR
   Create order request
   ↓
   Attempt to insert into database
   ↓
   Database error (connection lost)
   ↓
   Catch error in try/catch
   ↓
   Log error details
   ↓
   Return 500 Internal Server Error
   ↓
   Send user-friendly message
```

---

## 🚀 Deployment Flow

```
DEVELOPMENT → PRODUCTION

1. LOCAL DEVELOPMENT
   ┌─────────────────────┐
   │  Developer Machine  │
   │  - Edit code        │
   │  - Test locally     │
   │  - npm run dev      │
   └─────────────────────┘
            │
            ↓ git push
   ┌─────────────────────┐
   │   GitHub/GitLab     │
   │   - Code repository │
   └─────────────────────┘

2. DEPLOYMENT OPTIONS

   Option A: Heroku
   ┌─────────────────────┐
   │  git push heroku    │
   │  ↓                  │
   │  Heroku builds      │
   │  ↓                  │
   │  Starts server      │
   │  ↓                  │
   │  Public URL ready   │
   └─────────────────────┘

   Option B: Railway
   ┌─────────────────────┐
   │  Connect GitHub     │
   │  ↓                  │
   │  Auto-deploy        │
   │  ↓                  │
   │  SSL included       │
   │  ↓                  │
   │  Public URL ready   │
   └─────────────────────┘

   Option C: VPS (DigitalOcean/AWS)
   ┌─────────────────────┐
   │  SSH to server      │
   │  ↓                  │
   │  git clone          │
   │  ↓                  │
   │  npm install        │
   │  ↓                  │
   │  pm2 start          │
   │  ↓                  │
   │  Configure Nginx    │
   │  ↓                  │
   │  Setup SSL          │
   └─────────────────────┘

3. POST-DEPLOYMENT
   ┌─────────────────────┐
   │  Configure webhooks │
   │  in Ghala Dashboard │
   │  ↓                  │
   │  Test endpoints     │
   │  ↓                  │
   │  Monitor logs       │
   │  ↓                  │
   │  Ready for users! ✅│
   └─────────────────────┘
```

---

## 📊 Monitoring & Logging Flow

```
APPLICATION EVENTS
┌──────────────────────────────────────────────────────────┐
│  - Incoming message                                      │
│  - Order created                                         │
│  - Payment initiated                                     │
│  - Webhook received                                      │
│  - Error occurred                                        │
└──────────────────────────────────────────────────────────┘
       │
       ↓ logger.info() / logger.error()
┌──────────────────────────────────────────────────────────┐
│  LOGGER (utils/logger.js)                                │
│  - Formats log message                                   │
│  - Adds timestamp                                        │
│  - Adds log level                                        │
└──────────────────────────────────────────────────────────┘
       │
       ↓
┌──────────────────────────────────────────────────────────┐
│  CONSOLE OUTPUT                                          │
│  [INFO] 2026-01-16T10:30:00.000Z Order created: ORD-123  │
│  [ERROR] 2026-01-16T10:31:00.000Z Payment failed        │
└──────────────────────────────────────────────────────────┘
       │
       ↓ (if using PM2)
┌──────────────────────────────────────────────────────────┐
│  PM2 LOG FILES                                           │
│  - ~/.pm2/logs/whatsapp-sales-out.log                    │
│  - ~/.pm2/logs/whatsapp-sales-error.log                  │
└──────────────────────────────────────────────────────────┘
       │
       ↓ View with: pm2 logs
┌──────────────────────────────────────────────────────────┐
│  DEVELOPER/ADMIN                                         │
│  - Monitor application health                            │
│  - Debug issues                                          │
│  - Track performance                                     │
└──────────────────────────────────────────────────────────┘
```

---

## 🎯 Key Takeaways

### Data Flow Summary
1. **Customer → WhatsApp → Ghala → Our Server**
2. **Our Server → Ghala → WhatsApp → Customer**
3. **Ghala → Webhook → Our Server → Database**

### Security Layers
1. ✅ Webhook signature verification
2. ✅ Basic authentication for admin
3. ✅ SQL injection prevention (parameterized queries)
4. ✅ Environment variable protection

### Scalability Points
- Use Redis for user state (instead of in-memory Map)
- Implement queue system for webhook processing
- Add database connection pooling
- Use CDN for static assets
- Implement rate limiting

### Critical Paths
1. **Order Creation**: Customer → Message → Order → Database
2. **Payment**: Order → Ghala → M-Pesa → Webhook → Receipt
3. **Admin Dashboard**: Browser → API → Database → UI

---

**This workflow documentation provides a complete visual understanding of how the WhatsApp Micro-Sales Assistant operates from end to end.**
