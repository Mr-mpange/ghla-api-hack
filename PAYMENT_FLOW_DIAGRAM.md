# Payment Flow Diagram

## Complete Payment Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                     CUSTOMER JOURNEY                                 │
└─────────────────────────────────────────────────────────────────────┘

1. BOOKING PHASE
   ┌──────────┐
   │ Customer │
   └────┬─────┘
        │ "Browse Cars"
        ▼
   ┌──────────────┐
   │  WhatsApp    │
   │  Bot Service │
   └────┬─────────┘
        │ Shows car categories
        ▼
   ┌──────────────┐
   │   Customer   │
   │ Selects Car  │
   └────┬─────────┘
        │ Provides booking details
        ▼
   ┌──────────────────┐
   │  Booking Created │
   │  Status: confirmed│
   └────┬─────────────┘
        │
        ▼

2. PAYMENT INITIATION
   ┌──────────────────┐
   │ Customer clicks  │
   │   "Pay Now"      │
   └────┬─────────────┘
        │
        ▼
   ┌──────────────────────────┐
   │ carRentalBotService      │
   │ .initiateSnippePayment() │
   └────┬─────────────────────┘
        │ POST /v1/payments
        ▼
   ┌──────────────────┐
   │  Snippe API      │
   │  Creates Payment │
   └────┬─────────────┘
        │ Returns payment_id
        ▼
   ┌──────────────────────┐
   │ Payment Instructions │
   │ sent to Customer     │
   └────┬─────────────────┘
        │
        ▼

3. CUSTOMER PAYMENT
   ┌──────────────────┐
   │ Customer Phone   │
   │ Receives USSD    │
   └────┬─────────────┘
        │ Enters PIN
        ▼
   ┌──────────────────┐
   │ Mobile Money     │
   │ Provider         │
   └────┬─────────────┘
        │ Processes payment
        ▼
   ┌──────────────────┐
   │ Payment Complete │
   └────┬─────────────┘
        │
        ▼

4. WEBHOOK NOTIFICATION
   ┌──────────────────┐
   │  Snippe API      │
   │  Sends Webhook   │
   └────┬─────────────┘
        │ POST /webhook/snippe/payment
        ▼
   ┌──────────────────────────┐
   │ Express Server           │
   │ Verifies Signature       │
   └────┬─────────────────────┘
        │ Signature valid
        ▼
   ┌──────────────────────────┐
   │ carRentalBotService      │
   │ .handlePaymentWebhook()  │
   └────┬─────────────────────┘
        │ Updates booking
        ▼
   ┌──────────────────────┐
   │ Booking Status:      │
   │ "paid"               │
   └────┬─────────────────┘
        │
        ▼

5. CONFIRMATION
   ┌──────────────────────────┐
   │ whatsappResponseService  │
   │ .sendTextMessage()       │
   └────┬─────────────────────┘
        │ Sends confirmation
        ▼
   ┌──────────────────┐
   │ Customer         │
   │ Receives         │
   │ Confirmation 🎉  │
   └──────────────────┘
```

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        SYSTEM COMPONENTS                         │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────┐
│   WhatsApp   │◄───────►│    Express   │◄───────►│   Snippe     │
│   Business   │         │    Server    │         │     API      │
│     API      │         │              │         │              │
└──────────────┘         └──────┬───────┘         └──────────────┘
                                │
                                │
                    ┌───────────┼───────────┐
                    │           │           │
                    ▼           ▼           ▼
            ┌──────────┐ ┌──────────┐ ┌──────────┐
            │ WhatsApp │ │   Car    │ │  Snippe  │
            │ Response │ │  Rental  │ │ Payment  │
            │ Service  │ │   Bot    │ │ Service  │
            └──────────┘ └──────────┘ └──────────┘
```

## Data Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                          DATA FLOW                               │
└─────────────────────────────────────────────────────────────────┘

BOOKING DATA
┌──────────────────────────────────────┐
│ {                                    │
│   id: "BK1234567890",                │
│   customerId: "+255683859574",       │
│   carId: "eco_001",                  │
│   carName: "Toyota Vitz",            │
│   totalAmount: 5000,                 │
│   deposit: 2500,                     │
│   status: "confirmed",               │
│   paymentId: null,                   │
│   paymentStatus: null                │
│ }                                    │
└──────────────────────────────────────┘
                │
                │ Payment Initiated
                ▼
PAYMENT REQUEST
┌──────────────────────────────────────┐
│ {                                    │
│   amount: 2500,                      │
│   currency: "TZS",                   │
│   phoneNumber: "+255683859574",      │
│   reference: "BK1234567890",         │
│   description: "Car Rental Deposit"  │
│ }                                    │
└──────────────────────────────────────┘
                │
                │ Snippe API
                ▼
PAYMENT RESPONSE
┌──────────────────────────────────────┐
│ {                                    │
│   paymentId: "pay_abc123",           │
│   status: "pending",                 │
│   reference: "BK1234567890",         │
│   phoneNumber: "+255683859574"       │
│ }                                    │
└──────────────────────────────────────┘
                │
                │ Customer Pays
                ▼
WEBHOOK NOTIFICATION
┌──────────────────────────────────────┐
│ {                                    │
│   event: "payment.completed",        │
│   data: {                            │
│     id: "pay_abc123",                │
│     status: "completed",             │
│     reference: "BK1234567890",       │
│     amount: 2500                     │
│   }                                  │
│ }                                    │
└──────────────────────────────────────┘
                │
                │ Update Booking
                ▼
UPDATED BOOKING
┌──────────────────────────────────────┐
│ {                                    │
│   id: "BK1234567890",                │
│   status: "paid",                    │
│   paymentId: "pay_abc123",           │
│   paymentStatus: "completed",        │
│   paymentDate: "2024-01-25T10:30:00" │
│ }                                    │
└──────────────────────────────────────┘
```

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                      ERROR SCENARIOS                             │
└─────────────────────────────────────────────────────────────────┘

SCENARIO 1: Payment Initiation Fails
┌──────────────┐
│ Pay Now      │
└──────┬───────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐
│ Snippe API   │────►│ Error        │
│ Returns Error│     │ (e.g. invalid│
└──────────────┘     │  phone)      │
                     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │ Show Error   │
                     │ Message      │
                     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │ Retry Button │
                     └──────────────┘

SCENARIO 2: Payment Pending
┌──────────────┐
│ Check Status │
└──────┬───────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐
│ Snippe API   │────►│ Status:      │
│ Check        │     │ "pending"    │
└──────────────┘     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │ Show Pending │
                     │ Message      │
                     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │ Check Again  │
                     │ Button       │
                     └──────────────┘

SCENARIO 3: Payment Failed
┌──────────────┐
│ Webhook      │
│ Received     │
└──────┬───────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐
│ Status:      │────►│ Update       │
│ "failed"     │     │ Booking      │
└──────────────┘     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │ Notify       │
                     │ Customer     │
                     └──────┬───────┘
                            │
                            ▼
                     ┌──────────────┐
                     │ Offer Retry  │
                     └──────────────┘
```

## Timeline

```
┌─────────────────────────────────────────────────────────────────┐
│                    TYPICAL PAYMENT TIMELINE                      │
└─────────────────────────────────────────────────────────────────┘

T+0s    Customer clicks "Pay Now"
        │
T+1s    Payment request sent to Snippe
        │
T+2s    USSD prompt sent to customer phone
        │
T+5s    Customer enters PIN
        │
T+8s    Mobile money provider processes
        │
T+10s   Payment completed
        │
T+11s   Webhook sent to server
        │
T+12s   Booking updated to "paid"
        │
T+13s   Confirmation sent to customer
        │
T+14s   Customer receives WhatsApp message ✅

Total Time: ~14 seconds
```

## Integration Points

```
┌─────────────────────────────────────────────────────────────────┐
│                    INTEGRATION POINTS                            │
└─────────────────────────────────────────────────────────────────┘

1. WhatsApp → Server
   Endpoint: POST /webhook/whatsapp
   Purpose: Receive customer messages

2. Server → Snippe
   Endpoint: POST https://api.snippe.sh/v1/payments
   Purpose: Create payment request

3. Snippe → Server
   Endpoint: POST /webhook/snippe/payment
   Purpose: Payment status notification

4. Server → WhatsApp
   Endpoint: POST https://graph.facebook.com/v18.0/{phone_id}/messages
   Purpose: Send confirmation to customer

5. Server → Snippe
   Endpoint: GET https://api.snippe.sh/v1/payments/{id}
   Purpose: Check payment status
```

---

**Visual Guide Complete!** 📊

This diagram shows the complete flow from booking to payment confirmation, including all system components and data flows.
